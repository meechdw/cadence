{
  "root": true,
  "tasks": {
    "test": {
      "aliases": ["t"],
      "cmd": "bun {{bun_opts}} test --timeout {{timeout}} {{test_opts}}",
      "params": {
        "timeout": "3000"
      }
    }
  }
}
