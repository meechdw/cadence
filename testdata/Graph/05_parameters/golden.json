{
  "task_names": ["t"],
  "params": ["bun_opts='--cwd src' test_opts=--coverage"],
  "expected_nodes": {
    "test": {
      "sub_path": ".",
      "task_name": "test",
      "cmd": "bun {{bun_opts}} test --timeout {{timeout}} {{test_opts}}",
      "params": {
        "timeout": "3000"
      },
      "cli_params": {
        "bun_opts": "--cwd src",
        "test_opts": "--coverage"
      }
    }
  }
}
