{
  "input": {
    "task_names": ["t"],
    "params": ["opts=--coverage"]
  },
  "expected_nodes": {
    "test": {
      "sub_path": ".",
      "task_name": "test",
      "cmd": "bun test --timeout {{timeout}} {{opts}}",
      "params": {
        "timeout": "3000"
      },
      "cli_params": {
        "opts": "--coverage"
      }
    }
  }
}
